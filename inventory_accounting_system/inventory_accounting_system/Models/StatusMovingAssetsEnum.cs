namespace inventory_accounting_system.Models {
    public enum StatusMovingAssetsEnum {
        sold,
        transfer,
        write_off,
        transfer_in,
        transfer_out,
        short_file,

    }
}